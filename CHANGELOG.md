# Changelog

## Unreleased

### Merged pull requests

- [#1](https://github.com/Micro1-expert-mock/mock-ci-agent-challenge/pull/1) `[codex] Fix cart lint and discount logic` - removes the unused `debugMode` variable, adds the missing semicolon in `calculateSubtotal`, and fixes percentage handling in `applyDiscount`.
